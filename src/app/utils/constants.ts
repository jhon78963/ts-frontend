import { environment } from '../../environments/environment';

export const BASE_URL = environment.BASE_URL;
export const COMPANY_ID = environment.COMPANY_ID;

export const BASE_FILE_URL = environment.BASE_FILE_URL;
export const FILE_TOKEN = environment.ACCESS_FILE_TOKEN;
export const BASE_S3_URL = environment.BASE_S3_URL;
